{
  "name": "truck-load-payment-sheet",
  "version": "0.1.0",
  "private": true,
  "description": "MERN stack truck-load payment sheet system",
  "workspaces": [
    "backend/server",
    "frontend/client",
    "shared/utils"
  ],
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend/server\" \"npm run dev --prefix frontend/client\"",
    "test": "npm test --prefix backend/server",
    "build": "npm run build --prefix frontend/client"
  }
}
